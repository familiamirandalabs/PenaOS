# PenaOS: garante que o servidor X pode subir (a sessao roda como root no tty1).
# Sem isto, em alguns Debian o 'startx' recusa com "only console users allowed".
allowed_users=anybody
needs_root_rights=yes
